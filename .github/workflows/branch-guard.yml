name: Branch Guard

on:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  contents: read
  pull-requests: read

jobs:
  guard:
    uses: WastelandWares/.github/.github/workflows/branch-guard.yml@main
